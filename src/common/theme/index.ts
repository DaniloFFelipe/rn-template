import { breakpoints } from './breakponits'
import { colors } from './colors'
import { fontFamily, fontSizes } from './font'
import { radii } from './radii'
import { spacings } from './spacings'

export const theme = {
  breakpoints,
  colors,
  radii,
  spacings,
  fontSizes,
  fontFamily,
}
